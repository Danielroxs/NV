import React from "react";

const Spinner = () => {
  return (
    <div
      className="spinner"
      role="status"
      aria-live="polite"
      aria-label="Cargando"
    ></div>
  );
};

export default Spinner;
